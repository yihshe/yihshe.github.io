source 'https://rubygems.org'

group :jekyll_plugins do
  gem 'jekyll'
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'webrick', '~> 1.8'
end

gem 'github-pages'

# Pinned for compatibility with the macOS system Ruby (2.6); newer ffi needs Ruby >= 3
gem 'ffi', '~> 1.16.3'
